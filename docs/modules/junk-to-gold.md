# Module: mod-junk-to-gold
installed: neither realm
notes: not present in either azerothcore-wotlk/modules (Sunstrider, empty) or playerbots-wotlk/modules (Illidan) as of 2026-07-29. Was on the compiled backlog as something touched, but no directory or CMake reference found on the deploy VM (root@<homelab-vm-lan-ip>). Likely discussed/planned but never actually installed.
