# Issue #20821 — No Wisps at Hidden Shrine in Ashenvale
state: open, labels: Confirmed, Fix included, DB Spawns
summary: reporter expected static "Small/Medium/Large Wisp" gameobject spawns at the Hidden Shrine and posted a static DELETE+INSERT of sniffed spawn coordinates as their proposed fix.
status: investigated, skipped -- proposed fix contradicted, unresolved
notes: the "fix" in the label is the issue reporter's own opening-post SQL, not a maintainer/verified-commenter confirmation. The comment thread actually pushes back on it: contributor TheSCREWEDSoftware points to a video showing the wisps only appear for a limited window after a specific quest turn-in, and member sudlud agrees that matches sniff behavior -- i.e. these are conditional/temporary spawns (SmartAI summon-on-event), not permanent static gameobject rows. Applying the reporter's static INSERT as posted would misrepresent the actual blizzlike behavior per the people who looked closest at it. No resolved fix exists in the thread; skipping.
