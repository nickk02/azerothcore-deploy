# Issue #23092 — [QUEST] [WOTLK] 12818 - Clean Up - gameobject spawns incomplete
state: open, labels: Fix included, 75-79
summary: quest 12818 "Clean Up" wreckage-part gameobjects (191530-191537, "Charred Wreckage") are missing a bunch of spawn locations compared to sniffs, and at each location one of 8 possible wreckage entries should spawn randomly (i.e. pooled).
status: investigated, skipped -- fix retracted by its own author as unimplementable
notes: member sudlud posted a large sniffed spawn list (guid column left blank, meaning "insert and let auto_increment assign"), but immediately followed up in the next comment: "now that i think about it, this is probably one of th[o]se things not supported yet on ac -- a pool of gameobjects that should respawn after a given amount of time after use." AzerothCore's gameobject pooling doesn't support that respawn-after-use-with-random-entry-choice behavior, so a straight static INSERT of the sniffed list (which is all that's concretely given) would not reproduce blizzlike behavior -- it would just add a pile of always-up wreckage piles instead of the intended rotating pool. No usable DB-only fix exists in the thread as of 2026-07-30; the author of the proposed data flagged it as insufficient themselves. Skipping.
