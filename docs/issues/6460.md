# Issue #6460 — [Elwynn Forest] Missing Chest Pooling
state: open, labels: Confirmed, 1-19, DB
summary: multiple "Battered Chest" (entry 106318) gameobjects are simultaneously active per sub-zone in Elwynn Forest (Murloc Encampment, Brackwell Pumpkin Patch, Jerod's Landing, Fargodeep Mine, Forest's Edge); retail only has one active per sub-zone at a time. Needs more gameobject_pool entries.
status: investigated, skipped -- no concrete fix in thread
notes: the issue body itself is thorough (per-subzone GUIDs and coordinates for every duplicate chest), but that's raw diagnostic data from the reporter, not a proposed fix. Nobody in the thread (the only comment is an unrelated "reopened" note linking a chromiecraft re-report) posted the actual `pool_template`/`pool_gameobject` rows -- pool size, chance weighting per GUID, which ones are the intended defaults. Building that pooling SQL myself from the raw GUID list would mean inventing the pool design rather than applying someone else's verified fix, which is out of bounds for this sweep. Related to the already-closed #4496. Skipping until someone posts the actual pool rows upstream.
