# Issue #14209 — A bunch of missing quest_template_locale entries.
state: open, labels: Quest, DB, Fix included, Good first issue
summary: a SELECT against `quest_template_locale` finds 4,175 quest entries with missing localized text across one or more locale columns.
status: investigated, skipped -- data dump exists, too broad to apply safely in this pass
notes: contributor heyitsbench did the legwork -- sniffed and released a full SQL dump (linked zip, ~10306600) plus a maintained Google Sheet, covering thousands of quest_template_locale rows across multiple locales including a ptBR set. This is real, usable community-sourced data, not vague guidance. But it's a bulk unreviewed dataset against 4,175+ rows, not a single verifiable fix -- applying it properly means cross-referencing each row against current quest_template content (IDs shift, quest text gets touched by other PRs over time) to avoid silently overwriting or drifting from current quest data on either realm. That's a project-sized task, not a sweep-sized one. Flagging as a legitimate future project rather than attempting a partial, unverified slice of it here.
