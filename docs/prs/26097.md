# PR #26097 — actually Issue #26097: Thunderlord Clan Artifacts don't drop from ogres
upstream: issue #26097 (open) at azerothcore/azerothcore-wotlk, not a PR -- there is no PR #26097, which is why `gh pr view 26097` 404s
type: sql-only, hand-authored (not sourced from a merged upstream PR)
applied: sunstrider (acore_world), illidan (acore_pb_world)
status: applied
verified-in-game: no
tables: creature_loot_template
evidence: `gh issue view 26097 --repo azerothcore/azerothcore-wotlk` returns a real, still-open issue titled "[NPC][QUEST]Thunderlord Clan Artifacts doesn't drop from ogres" (Entry 19995/19998 referenced directly in the issue's repro steps). `nickk02/azerothcore-ops` round2sql-archive/26097.sql contains three hand-authored `UPDATE creature_loot_template SET Chance = ...` statements for Item 30431 on Entry 19995/19998/20728. Live DB check on 2026-07-30 confirms both acore_world and acore_pb_world currently have Chance=4, 4, 3 on those exact rows -- matching the SQL's SET values exactly, on both realms.
notes: the compiled backlog conflated an upstream GitHub Issue number with a PR number -- issue and PR numbers share one sequence on the same repo, and whoever compiled the backlog grabbed 26097 off the issue tracker without noticing it was never turned into a PR. The fix itself is real and was hand-authored during the round-2 accuracy sweep rather than pulled from any upstream patch (the upstream issue is still open, unfixed, no PR ever opened against it). Correcting status from unknown to applied since the DB evidence confirms the fix is live on both realms.
