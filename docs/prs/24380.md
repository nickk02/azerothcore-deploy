# PR #24380 — actually Issue #24380: Call to Arms shows Alliance flags on the Horde side in Dalaran
upstream: issue #24380 (open) at azerothcore/azerothcore-wotlk, not a PR -- there is no PR #24380, which is why `gh pr view 24380` 404s
type: issue-only -- no PR, no archived SQL, no fix I could find evidence of
applied: none
status: skipped
verified-in-game: no
tables: gameobject, gameobject_template -- inconclusive on the specific faction/flag bug
evidence: `gh issue view 24380 --repo azerothcore/azerothcore-wotlk` returns a real, still-open issue titled "[Call to Arms] - Alliance flags on the horde side in Dalaran, it shouldn't" (issue author themselves wasn't sure which Call to Arms GO was wrong). `nickk02/azerothcore-ops` round2sql-archive/ has no `24380.sql`. The only `gameobject_template` row named "Call to Arms!" is entry 179913, with a single spawn (guid 46197) on map 0 (Eastern Kingdoms), not map 571 (Dalaran) -- so on this server the specific per-faction Dalaran flag pair the issue describes doesn't obviously exist as distinct spawns to check at all, on either realm.
notes: same root cause as the other issue-number mixups. Genuinely unresolved: no local fix was ever attempted, and the DB layout doesn't cleanly map to the bug report (may be a phasing/conditions issue rather than a spawn-table one). Leaving as skipped rather than guessing at a resolution.
