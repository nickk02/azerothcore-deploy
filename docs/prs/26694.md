# PR #26694 — actually Issue #26694: heroic "(1)" creatures missing pickpocket loot
upstream: issue #26694 (open) at azerothcore/azerothcore-wotlk, not a PR -- there is no PR #26694, which is why `gh pr view 26694` 404s
type: sql-only, hand-authored (not sourced from a merged upstream PR)
applied: sunstrider (acore_world), illidan (acore_pb_world)
status: applied
verified-in-game: no
tables: creature_template (pickpocketloot)
evidence: `gh issue view 26694 --repo azerothcore/azerothcore-wotlk` returns a real, still-open issue titled "[ROGUE]'some' mobs arent pickpocketable in heroics but are in normal dungeon". `nickk02/azerothcore-ops` round2sql-archive/pickpocket-systemic.sql opens with the comment "-- Issue #26694: heroic '(1)' creature variants are missing pickpocketloot" and copies pickpocketloot from the exact-name-matching normal-mode sibling wherever the match is unambiguous. Re-running that file's own SELECT against both realms on 2026-07-30 finds zero remaining unfixed rows, and a positive check (heroic variant pickpocketloot now equal to its normal sibling's nonzero value) finds 343 matching rows on both acore_world and acore_pb_world -- the fix is live on both realms.
notes: same root cause as #26097 -- the compiled backlog pulled 26694 off the issue tracker and never verified it was a PR, so it never was one. The earlier note in this file speculating the number came from a creature entry cited in PR #26851's body was a red herring; the real source is the issue's own number, which the fix file's comment references directly. Correcting status from unknown to applied.
