# PR #13322 — actually Issue #13322: Lake Snappers missing from Lake Elrendar
upstream: issue #13322 (open) at azerothcore/azerothcore-wotlk, not a PR -- there is no PR #13322, which is why `gh pr view 13322` 404s
type: issue-only -- no PR, no archived SQL, no fix exists anywhere I could find
applied: none
status: skipped
verified-in-game: no
tables: creature (spawns), creature_template
evidence: `gh issue view 13322 --repo azerothcore/azerothcore-wotlk` returns a real, still-open issue titled "[Eversong Woods] Lake Snappers are missing from Lake Elrendar" ("The turtles are missing" / "Present" expected). `nickk02/azerothcore-ops` round2sql-archive/ has no `13322.sql`, unlike #26097 and #26694. creature_template has a `Lake Snapper` entry (33710) on both acore_world and acore_pb_world, but a spawn-table check for `creature WHERE id=33710` on 2026-07-30 returns zero rows on both realms -- the turtles genuinely have no spawns anywhere, confirming the bug is present and unaddressed.
notes: same root cause as #26097/#26694 -- an upstream Issue number got compiled into the PR backlog. Unlike those two, this one was never hand-fixed: there's no archived SQL and the DB confirms the reported bug is still live on both realms. Correcting status from unknown to skipped (identified, confirmed unfixed, nothing to apply).
