# PR #19679 — actually Issue #19679: Bloodmyst Isle bridge trigger missing (The Kessel Run)
upstream: issue #19679 (open) at azerothcore/azerothcore-wotlk, not a PR -- there is no PR #19679, which is why `gh pr view 19679` 404s
type: issue-only -- no PR, no archived SQL, no fix I could find evidence of
applied: none
status: skipped
verified-in-game: no
tables: quest_template (9663 "The Kessel Run") -- exact trigger object not identified this pass
evidence: `gh issue view 19679 --repo azerothcore/azerothcore-wotlk` returns a real, still-open issue titled "[Bloodmyst Isle] Trigger on the bridge is missing during The Kessel Run quest". `nickk02/azerothcore-ops` round2sql-archive/ has no `19679.sql`. Quest 9663 ("The Kessel Run") exists identically on both realms; its RequiredNpcOrGo fields point at quest-turn-in NPCs (17440 High Chief Stillpine, 17116 Exarch Menelaous), not the missing bridge trigger itself, and there are no `conditions` rows keyed to quest 9663 on acore_world. I did not track down the specific areatrigger/GO the issue is describing -- that would need a sniff or the original ChromieCraft report linked from the issue to pin down.
notes: same root cause as the other issue-number mixups in this ledger. This one is genuinely unresolved either way: no local SQL was ever written for it, and I could not conclusively prove or disprove the trigger's presence with the DB checks run this pass. Leaving as skipped (nothing applied) rather than claiming a fix status I can't back up -- if this needs to actually get fixed, it needs a fresh look starting from the linked ChromieCraft report, not a re-read of this note.
