# PR #26375 — Move custom spell attributes from core to database
upstream: merged 2026-07-25
type: paired
applied: sunstrider 2026-07-2X (SQL half only), illidan 2026-07-2X (SQL half only)
status: blocked
verified-in-game: no
tables: spell_custom_attr
evidence: SELECT confirmed on both realms -- spell_custom_attr rows for 58984 (128), 45537 (2048), 6197 (16), etc. match the new data-driven values. The C++ half (removing the old hardcoded switch in SpellMgr.cpp and reading from spell_custom_attr instead) is NOT part of the eleven cherry-picked PRs in the running Sunstrider binary (see findings/cpp-deploys.md), and Illidan's stock build predates this PR entirely.
notes: blocked on a C++ deploy. SQL data is in both DBs but sits unused until a worldserver binary that reads spell_custom_attr for these ids is deployed; the old hardcoded behavior is what's actually in effect right now on both realms.
