# PR #26763 — The Avalanche sub-Zone Improvements Part 1: Bythius, Cerberon, Glonn
upstream: open (not merged) at azerothcore/azerothcore-wotlk as of 2026-07-30 -- `gh pr view 26763 --repo azerothcore/azerothcore-wotlk` resolves fine; the earlier 404 in this ledger was a search miss, not a fabricated number
type: sql-only
applied: sunstrider (acore_world), illidan (acore_pb_world)
status: applied
verified-in-game: no
tables: creature, creature_addon, waypoint_data, creature_formations, creature_template, smart_scripts
evidence: `gh search prs --repo azerothcore/azerothcore-wotlk "avalanche"` and `gh pr view 26763 --repo azerothcore/azerothcore-wotlk` both confirm this is a real, currently open PR by Voxstrasza, companion to the already-applied #26764 (Part 2). `nickk02/azerothcore-ops` round2sql-archive/26763.sql repositions creature guid 100735 (entry 28212) onto a new waypoint path (id 1007350, 14 points), rewrites `creature_formations` for the 100735/100431/100704 group, sets `creature_template.AIName` to SmartAI for entry 28212, and deletes its old `smart_scripts` ids 0/1. Live DB check on 2026-07-30 confirms exact matches on both acore_world and acore_pb_world: creature 100735 at position_x=6093.82/position_y=3697.64/position_z=121.38/orientation=1.6121/MovementType=2, all three creature_formations rows present with the SQL's exact dist/angle/groupAI values, all 14 waypoint_data rows present, and smart_scripts for entryorguid=28212 down to just id=2 (ids 0/1 gone). The fix is live on both realms.
notes: this one was never a fabricated number -- it's a real, open (not yet merged) upstream PR, and the SQL from it was already applied locally ahead of the upstream merge (same pattern as several other "applied, PR still open" rows in this ledger). Correcting status from unknown to applied and sunstrider/illidan from "inconclusive" to yes/yes.
